import './EditEvent.scss';

function EditEvent(props) {
    console.log(props);
    return (
        <main>
            <h2>dummy content for now</h2>
        </main>
    )
}

export default EditEvent;